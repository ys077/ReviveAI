/**
 * Seed IDs that produce each simulated execution outcome.
 * Derived from executeRecoveryStrategy() on the original dataset — not UI copy.
 */
export const DEMO_REFERENCES = {
  recovered: 'TXN-1001',
  alternativeRequired: 'TXN-1011',
  deferred: 'TXN-1002',
}
